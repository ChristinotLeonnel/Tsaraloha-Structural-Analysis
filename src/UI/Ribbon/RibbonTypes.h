#pragma once

namespace TSA::UI
{

enum class RibbonButtonSize
{
    Large,  // 32x32 icône avec libellé en-dessous (Commandes principales)
    Small,  // 16x16 icône avec libellé à côté ou icône seule (Commandes secondaires)
    Medium  // 24x24 icône avec libellé
};

} // namespace TSA::UI
